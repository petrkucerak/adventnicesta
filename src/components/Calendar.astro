---
import { getCollection } from "astro:content";

const allContemplation = await getCollection("contemplation");
console.log(allContemplation);
---

<div class="mx-6 my-6">
  <h2 class="text-2xl font-bold uppercase mb-1">Kalendář</h2>
  {
    /* <h3 class="text-xl font-bold ">Listopad</h3>
   <div class="grid grid-cols-5 auto-cols-max gap-y-2">
     {posts.map((post) => {
       const date_tmp = new Date(post.date);
       if (date_tmp.getMonth() === 10)
         return <Day key={post.slug} post={post} />;
     })}
   </div> */
  }
  <!-- <h3 class="text-xl font-bold ">Prosinec</h3>
   <div class="grid grid-cols-5 auto-cols-max gap-y-2">
     {posts.map((post) => {
       const date_tmp = new Date(post.date);
       if (date_tmp.getMonth() === 11)
         return <Day key={post.slug} post={post} />;
     })}
   </div> -->
</div>
